# wallet-service
A light-weight wallet system built with Node.js + TypeScript, supporting wallet creation, balance updates (credit/debit), transaction history, and strong decimal precision enforcement (up to 4 places). The project uses in-memory storage and includes atomic concurrency-safe updates using per-wallet locking.
